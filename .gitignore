# Raw/personal WHOOP data - not for version control
physiological_cycles.csv
workouts.csv
sleeps.csv

# Streamlit secrets (contains the same personal WHOOP data as a fallback for
# cloud deployment) - must never be committed. Paste its contents into the
# Streamlit Community Cloud app's own "Secrets" settings instead.
.streamlit/secrets.toml

# Logs
streamlit.log
*.log

# Python
__pycache__/
*.pyc
.venv/
venv/

# Local tooling config
.claude/

# Reference copy of Streamlit Cloud secrets content - never committed
deploy_secrets_reference/
